转化率
总的历史购买量
该用户是否已经购买过该商品
该用户前2/5天动作了该商品的次数
该用户最近一次动作该商品的时间

SVM
NB
xgboost
4x5000
几种模型概率加权
先预测user，再预测pair
不能一次性把全部feature放进模型？
正负1：10
利用最近5天的pair中的userid和skuid构建预测集合
利用最近5天的pair构建预测集合
一边等着算法·跑结果，一边学习理论知识。比如遇到的问题，以及记事本的问题
SMOTE（计算代价太高）
10个模型，每个十分之一
100个模型，每个模型用百分之一的负例。最后将100个模型的结果结合起来
如果是两步走，skuid用哪些？
看了阿里移动推荐的github的代码介绍，发现他们都是这样做的。ppt的大神发邮件要代码也不回。

数据平滑方式 自然对数 改为 e
如何更好的表示时间？如果独热编码太稀疏。不要日期特征。

xgboosting 参数设置权值 设置输出阈值

本地换编译器 pycharm-> spyder,notebook,sublime,atom

python memory error
推荐系统 数据挖掘 滑动窗口 特征统计
服务器线程被杀死
如何构造训练样本（和我的一样）
IJICAI 第一的gitlab
IJICAI 16的皮皮虾 github

常问问题及其回答